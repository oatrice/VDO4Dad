import { test, expect } from '@playwright/test';

test.describe('Download flow (legacy)', () => {
    test('placeholder - kept for compatibility', async ({}) => {
        // This spec has been split into download.mock.spec.ts and download.real.spec.ts
        // Keeping a no-op test to avoid sudden suite changes in some CI setups.
        expect(true).toBeTruthy();
    });
});


